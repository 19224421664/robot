# Log verbosity: debug | info | warn | error
LOG_LEVEL=info

# Port for the built-in health-check HTTP server
HEALTH_PORT=3000

# Retry configuration for scheduled tasks
RETRY_MAX_ATTEMPTS=3
RETRY_DELAY_MS=1000
